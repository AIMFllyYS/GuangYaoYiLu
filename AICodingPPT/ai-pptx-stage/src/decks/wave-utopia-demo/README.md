# wave-utopia-demo

这是 AI PPTX Stage 的标准示例 deck，用于验证固定幻灯片画布、画布外元素、跨页 Morph、Inspector 参数和 PPTX 手工复刻清单。

创作时只修改本目录下的 `deck.ts`、`theme.ts`、`assets/`、`pages/*/page.ts` 和 `pages/*/notes.md`。系统渲染器、编辑器、Morph 引擎和校验脚本不属于本 deck 的创作区。
