interface:
  display_name: "PPTX-Layer-merge"
  short_description: "Build premium editable PPTX decks with image-first layers"
  default_prompt: "Use $pptx-layer-merge to turn my materials and requirements into a premium image-first, layer-merged, editable PPTX deck."
